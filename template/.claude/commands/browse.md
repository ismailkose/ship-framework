Run /review eye-only with screenshot mode.

This is an alias for the Eye (Visual QA) lens inside /review. It runs only the visual QA pass — screenshots, design token comparison, mobile viewport check, interaction walkthrough, and visual bug checklist. No product review, no design audit, no adversarial challenge.

Use this when you just want to see what the user sees.

User's request: $ARGUMENTS
